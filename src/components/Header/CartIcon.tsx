import { MdShoppingCart } from 'react-icons/md';

export default function CartIcon() {
  return (
    <div>
      <MdShoppingCart
        size={20}
        className='relative'
      />
    </div>
  );
}
