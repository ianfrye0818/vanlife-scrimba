export interface Van {
  id: string;
  name: string;
  price: number;
  description?: string;
  imageURL?: string;
  type: string;
}

//TODO - move this somewhere else - doens't make sense to be here.
